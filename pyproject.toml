[metadata]
name = "timethese"
aAdded uthors = ["Joachim Bargsten <jw@bargsten.org>"]
license = "MIT"

[build-system]
requires = ["setuptools", "wheel"]
# build-backend = "setuptools.build-meta"

[tool.black]
line-length = 110
target-version = ['py35']
